{
  "name": "lumiverse-megumin-suite",
  "version": "9.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/backend.js --outfile dist/backend.js --target bun && bun build src/frontend.js --outfile dist/frontend.js --target browser",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test && bun run build"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.8.3",
    "lumiverse-spindle-types": "^0.5.13"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^7.2.0",
    "@fortawesome/free-solid-svg-icons": "^7.2.0",
    "jquery": "3.7.1"
  }
}
